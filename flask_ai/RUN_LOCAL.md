# تشغيل زحل AI محلياً

## المشكلة الحالية

الـ Flask AI غير مُعد على السيرفر، لذلك الواجهة الأمامية تعود للمحرك المحلي.

## الحل: تشغيل Flask AI محلياً

### 1. تثبيت المتطلبات

```bash
cd backend/flask_ai
pip install -r requirements.txt
```

### 2. تشغيل الخادم

```bash
python server.py
```

### 3. التحقق من التشغيل

افتح المتصفح على: `http://localhost:3001/api/ai/health`

يجب أن ترى:

```json
{
  "ok": true,
  "model_name": "microsoft/DialoGPT-medium",
  "features": {
    "smart_search": true,
    "context_management": true,
    "product_comparison": true,
    "recommendations": true,
    "nlp_processing": true
  }
}
```

### 4. اختبار الـ Chat

```bash
curl -X POST http://localhost:3001/api/ai/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "مرحبا", "session_id": "test"}'
```

## الميزات المتاحة محلياً

### 🤖 زحل AI الذكي

- **البحث الذكي**: فهم الطلبات بالعربي والإنجليزي
- **إدارة السياق**: تذكر المحادثات والتفضيلات
- **الاقتراحات الذكية**: منتجات مخصّصة
- **مقارنة المنتجات**: مقارنة شاملة
- **معالجة اللغة الطبيعية**: فهم عميق للطلبات

### 🔗 API Endpoints المحلية

#### Chat API

```
POST http://localhost:3001/api/ai/chat
{
  "message": "بدي موبايل رخيص",
  "session_id": "user_123",
  "history": [...]
}
```

#### Health Check

```
GET http://localhost:3001/api/ai/health
```

#### Test API

```
POST http://localhost:3001/api/ai/test
{
  "message": "مرحبا"
}
```

## استكشاف الأخطاء

### 1. فحص المنافذ

```bash
netstat -an | findstr :3001
```

### 2. فحص العمليات

```bash
tasklist | findstr python
```

### 3. سجلات الخادم

تحقق من console logs للخادم لرؤية:

- تحميل النموذج
- طلبات API
- أخطاء الاتصال

## الأداء المتوقع

- **الذاكرة**: ~2-4GB للنموذج الكامل
- **السرعة**: 1-3 ثواني للرد الأول
- **الكاش**: 2 ساعة للردود المتكررة
- **Redis**: اختياري لتحسين الأداء

## بعد التشغيل

1. **افتح الواجهة الأمامية** في المتصفح
2. **اضغط على زر زحل AI** في الزاوية السفلية
3. **اكتب رسالة** وسترى:
   - 🤖 **زحل AI الذكي** إذا كان Flask AI يعمل
   - ⚡ **محرك محلي** إذا كان Flask AI غير متاح

## نصائح للتشغيل

### 1. تشغيل في الخلفية (Windows)

```bash
start /B python server.py
```

### 2. تشغيل مع إعادة التشغيل التلقائي

```bash
pip install watchdog
python -m watchdog server.py
```

### 3. تشغيل على منفذ مختلف

```bash
set PORT=3002
python server.py
```

## الميزات المتقدمة

### 1. البحث الذكي

- فهم المرادفات (موبايل = جوال = هاتف)
- استخراج معايير البحث (السعر، الماركة)
- ترتيب النتائج حسب الملاءمة

### 2. إدارة السياق

- حفظ تفضيلات المستخدم
- فهم الإحالات السياقية ("شو رأيك فيه؟")
- تتبع المحادثات

### 3. الاقتراحات الذكية

- منتجات رائجة حسب المبيعات
- عروض ساخنة
- اقتراحات شخصية

### 4. مقارنة المنتجات

- جدول مقارنة شامل
- إبراز الفروقات الرئيسية
- ترتيب حسب المعايير المختلفة

