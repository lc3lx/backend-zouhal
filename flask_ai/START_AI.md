# تشغيل زحل AI الذكي

## متطلبات التشغيل

### 1. تثبيت المتطلبات

```bash
cd backend/flask_ai
pip install -r requirements.txt
```

### 2. متغيرات البيئة

انسخ `ENV_SAMPLE.txt` إلى `.env` وعدّل القيم:

```bash
cp ENV_SAMPLE.txt .env
```

### 3. تشغيل الخادم

```bash
python server.py
```

الخادم سيعمل على: `http://localhost:3001`

## الميزات المتاحة

### 🤖 زحل AI الذكي

- **البحث الذكي**: فهم الطلبات بالعربي والإنجليزي
- **إدارة السياق**: تذكر المحادثات والتفضيلات
- **الاقتراحات الذكية**: منتجات مخصّصة بناءً على التفضيلات
- **مقارنة المنتجات**: مقارنة شاملة بين المنتجات
- **معالجة اللغة الطبيعية**: فهم عميق للطلبات

### 🔗 API Endpoints

#### Chat API

```
POST /api/ai/chat
{
  "message": "بدي موبايل رخيص",
  "session_id": "user_123",
  "history": [...]
}
```

#### Search API

```
POST /api/ai/search
{
  "query": "موبايل سامسونج",
  "session_id": "user_123"
}
```

#### Compare API

```
POST /api/ai/compare
{
  "product_ids": ["id1", "id2", "id3"]
}
```

#### Similar Products API

```
POST /api/ai/similar
{
  "product_id": "product_id_here"
}
```

#### Health Check

```
GET /api/ai/health
```

#### Test API

```
POST /api/ai/test
{
  "message": "مرحبا"
}
```

## التكامل مع الواجهة الأمامية

الواجهة الأمامية تحاول الاتصال بـ Flask AI أولاً، وإذا فشل تعود للمحرك المحلي.

### ترتيب المحاولات:

1. `http://localhost:3001/api/ai/chat` (محلي)
2. `https://www.zuhall.com/api/ai/chat` (سيرفر)

### مؤشرات البصرية:

- 🤖 **زحل AI الذكي**: عندما يستخدم Flask AI
- ⚡ **محرك محلي**: عندما يستخدم المحرك المحلي

## استكشاف الأخطاء

### 1. فحص الاتصال

```bash
curl -X POST http://localhost:3001/api/ai/test \
  -H "Content-Type: application/json" \
  -d '{"message": "مرحبا"}'
```

### 2. فحص الصحة

```bash
curl http://localhost:3001/api/ai/health
```

### 3. سجلات الخادم

تحقق من console logs للخادم لرؤية:

- تحميل النموذج
- طلبات API
- أخطاء الاتصال

## الأداء

- **الذاكرة**: ~2-4GB للنموذج الكامل
- **السرعة**: 1-3 ثواني للرد الأول
- **الكاش**: 2 ساعة للردود المتكررة
- **Redis**: اختياري لتحسين الأداء

## الميزات المتقدمة

### 1. البحث الذكي

- فهم المرادفات (موبايل = جوال = هاتف)
- استخراج معايير البحث (السعر، الماركة)
- ترتيب النتائج حسب الملاءمة

### 2. إدارة السياق

- حفظ تفضيلات المستخدم
- فهم الإحالات السياقية ("شو رأيك فيه؟")
- تتبع المحادثات

### 3. الاقتراحات الذكية

- منتجات رائجة حسب المبيعات
- عروض ساخنة
- اقتراحات شخصية

### 4. مقارنة المنتجات

- جدول مقارنة شامل
- إبراز الفروقات الرئيسية
- ترتيب حسب المعايير المختلفة

